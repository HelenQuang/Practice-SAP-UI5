<mvc:View
  controllerName="sap.ui.demo.walkthrough.controller.Invoice"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc"
   displayBlock="true">
  <List headerText="{i18n>invoiceListTitle}" clas="sapUiResponsiveMargin" width="auto" items="{invoice>/Invoices}">
    <items>
      <ObjectListItem title="{invoice>Quantity} x {invoice>ProductName}" 
                      number="{
                    parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
                    type: 'sap.ui.model.type.Currency',
                    formatOptions: {
                        showMeasure: false,
                        preserveDecimals: false
                    }
                }"
                numberUnit="{view>/currency}"/>
    </items>
  </List>

</mvc:View>
<mvc:View
  controllerName="sap.ui.demo.walkthrough.controller.Invoice"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc"
   displayBlock="true">
  <List id="invoiceList" headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{
            path: 'invoice>/Invoices',
            sorter: {
                path: 'ShipperName',
                group: true
                }
            }">
    <headerToolbar>
      <Toolbar>
        <Title text="{i18n>invoiceListTitle}"/>
        <ToolbarSpacer/>
        <SearchField width="50%" search=".onFilterInvoices" placeholder="Search by product name"/>
      </Toolbar>
    </headerToolbar>
    <items>
      <ObjectListItem title="{invoice>Quantity} x {invoice>ProductName}" 
                      number="{
                    parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
                    type: 'sap.ui.model.type.Currency',
                    formatOptions: {
                        showMeasure: false,
                        preserveDecimals: false
                    }
                }"
                numberUnit="{view>/currency}" 
                numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Warning' : 'Information'}">
        <firstStatus>
          <ObjectStatus text="{
                        path: 'invoice>Status',
                        formatter: '.formatter.statusText'
                    }"/>
        </firstStatus>
      </ObjectListItem>
    </items>
  </List>

</mvc:View>